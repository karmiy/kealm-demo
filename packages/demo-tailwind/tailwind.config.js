module.exports = {
    // 清除选项
    purge: ['./src/**/*.tsx'],
    // darkMode: 'false', // or 'media' or 'class'
    // darkMode: 'class', // or 'media' or 'class'
    theme: {
        extend: {},
    },
    variants: {
        extend: {},
    },
};
