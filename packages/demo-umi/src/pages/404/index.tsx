import { history } from 'umi';
import styles from './styles.scss';

export default function ErrorPage() {
    return (
        <div>
            <h1 className={styles.title}>404 index</h1>
        </div>
    );
}
